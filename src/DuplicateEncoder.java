public class DuplicateEncoder
    {
       /* static String encode(String word)
        {
           /* int[] countArr = new int[256];
            String res = "";
            for(int i = 0; i < word.length(); i++)
            {
                char c = word.charAt(i);
                countArr[c - null]++;
            }

            for(int i = 0; i < word.length(); i++)
            {
                char c = word.charAt(i);
                int count = countArr[c - null];
                if(count == 1) {
                    res = res + "(";
                } else if (count > 1) {
                    res = res  + ")";
                }
            }


            return res;
        }*/
    }


